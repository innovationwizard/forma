/** Coverage / per-D31 "no drops" counts — banks, expenditures, counterparties, notes. */
import { runCategory } from "./_shared";
runCategory("Bank accounts");
runCategory("Coverage");
runCategory("Project metadata");
runCategory("Data quality flags");
runCategory("Audit log");
