# Real-life source data — never commit. Contains PII / financials / bank statements.
# Per `docs/README.md`, this directory is the holding's source data vault.
docs/

# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js build artifacts
/.next/
/out/
/build

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Env files — never commit secrets. Use .env.example as the documented template.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test

# Editor / OS noise
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local tooling scratchpads (e.g., /tmp/forma_inspect.py output)
*.local
